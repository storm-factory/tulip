var canvas = new fabric.Canvas('main');
//
var originX = 150;
var originY = 300;
var entryTrack = new Segment(originX, originY, originX, originY - 150, true);

// $(document).ready(function(){
//   var canvas =  $('#main');
//   paper.setup(canvas);
//   var entryTrack = new SegmentTwo(originX, originY, originX, originY - 150, true);
// });
